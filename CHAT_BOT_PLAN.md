# План интеграции чат-бота с ИИ

## 📋 Обзор проекта

Интеграция рабочего чат-бота в существующий компонент `Assistant.tsx`, который будет отправлять сообщения пользователя в ИИ через `aiService` и отображать ответы в реальном времени.

---

## 🎯 Цели

1. ✅ Сделать чат функциональным (сейчас только форма)
2. ✅ Интегрировать с существующим `aiService`
3. ✅ Сохранять историю сообщений
4. ✅ Обрабатывать ошибки и состояния загрузки
5. ✅ Поддержка контекста разговора

---

## 📁 Структура изменений

### 1. Backend (Server)

#### 1.1. Расширение `aiService.mjs`
**Файл:** `server/src/services/aiService.mjs`

**Добавить новую функцию:**
```javascript
/**
 * Обрабатывает сообщение пользователя в чате
 * @param {string} message - Сообщение пользователя
 * @param {number} userId - ID пользователя (опционально)
 * @param {Array} conversationHistory - История разговора (опционально)
 * @returns {Promise<string>} - Ответ от ИИ
 */
export async function chatWithAI(message, userId = null, conversationHistory = []) {
  // Логика обработки сообщения
}
```

**Особенности:**
- Использовать существующий `client` OpenAI
- Поддержка контекста разговора (история сообщений)
- Если есть `userId`, можно использовать данные пользователя для персонализации
- Системный промпт для музыкального ассистента
- **Автоматическое определение запросов на рекомендации:** если в сообщении есть слова "подскажи", "посоветуй" или "порекомендуй", автоматически вызывать `getTrackRecommedations(userId, 5)` и возвращать 5 рекомендаций треков

#### 1.2. Создание API endpoint для чата
**Файл:** `next/app/api/chat/route.ts` (новый файл)

**Функционал:**
- POST endpoint для отправки сообщений
- Получение `userId` из cookies
- Валидация входных данных
- Вызов `chatWithAI`
- Возврат ответа

**Структура запроса:**
```typescript
POST /api/chat
Body: {
  message: string,
  conversationHistory?: Array<{role: string, content: string}>
}
```

**Структура ответа:**
```typescript
{
  response: string,
  error?: string
}
```

---

### 2. Frontend (Next.js)

#### 2.1. Обновление Store
**Файл:** `next/app/stores/useAssistantStore.ts`

**Добавить:**
```typescript
interface Message {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  timestamp: Date;
}

interface AssistantState {
  isOpen: boolean;
  messages: Message[];
  isLoading: boolean;
  error: string | null;
  toggleAssistant: () => void;
  closeAssistant: () => void;
  addMessage: (message: Omit<Message, 'id' | 'timestamp'>) => void;
  sendMessage: (content: string) => Promise<void>;
  clearMessages: () => void;
  setLoading: (loading: boolean) => void;
  setError: (error: string | null) => void;
}
```

**Функции:**
- `addMessage` - добавление сообщения в историю
- `sendMessage` - отправка сообщения на сервер
- `clearMessages` - очистка истории
- `setLoading` - управление состоянием загрузки
- `setError` - управление ошибками

#### 2.2. Обновление компонента Assistant
**Файл:** `next/app/components/Assistant/Assistant.tsx`

**Изменения:**

1. **Добавить состояние:**
   - Интеграция с обновленным store
   - Обработка отправки сообщений
   - Отображение истории сообщений

2. **Область сообщений:**
   - Отображение всех сообщений из истории
   - Разделение сообщений пользователя и ассистента
   - Скролл к последнему сообщению
   - Индикатор загрузки при ожидании ответа

3. **Форма ввода:**
   - Обработка отправки (Enter и кнопка)
   - Блокировка во время загрузки
   - Очистка поля после отправки
   - Валидация (не отправлять пустые сообщения)

4. **UI улучшения:**
   - Анимации для новых сообщений
   - Индикатор печати (typing indicator)
   - Обработка ошибок с отображением сообщений

---

## 🎯 Специальная функциональность: Автоматические рекомендации

### Описание
Когда пользователь пишет в чате слова **"подскажи"**, **"посоветуй"** или **"порекомендуй"**, система автоматически:
1. Определяет, что это запрос на рекомендации
2. Вызывает функцию `getTrackRecommedations(userId, 5)` из `aiService.mjs`
3. Возвращает 5 персональных рекомендаций треков на основе любимых треков пользователя
4. Форматирует ответ в удобном виде

### Условия работы
- ✅ Должен быть `userId` (пользователь авторизован)
- ✅ В сообщении должно быть одно из ключевых слов
- ✅ У пользователя должны быть треки в базе данных

### Примеры запросов
- "Подскажи что послушать"
- "Посоветуй мне музыку"
- "Порекомендуй треки"
- "Можешь порекомендовать что-нибудь?"

### Обработка ошибок
- Если `userId` нет → работает обычный чат через ИИ
- Если нет треков в БД → возвращается сообщение об этом
- Если ошибка при получении рекомендаций → fallback на обычный чат

---

## 🔧 Детальный план реализации

### Этап 1: Backend - Расширение aiService

**Шаги:**
1. Открыть `server/src/services/aiService.mjs`
2. Добавить функцию `chatWithAI`
3. Реализовать логику:
   - Функция `isRecommendationRequest` для проверки ключевых слов
   - Условная логика: если запрос на рекомендации + есть userId → вызывать `getTrackRecommedations`
   - Формирование системного промпта для музыкального ассистента
   - Поддержка истории разговора
   - Опциональная персонализация через userId
   - Обработка ошибок (fallback на обычный чат при ошибках рекомендаций)

**Системный промпт (пример):**
```
"Ты музыкальный ассистент. Помогаешь пользователям с рекомендациями музыки, анализом вкусов и статистикой. Отвечай кратко и по делу."
```

**Логика определения запросов на рекомендации:**
- Проверять наличие ключевых слов в сообщении: "подскажи", "посоветуй", "порекомендуй" (регистронезависимо)
- Если найдено и есть `userId`, вызывать `getTrackRecommedations(userId, 5)`
- Возвращать результат рекомендаций как ответ ассистента
- Если `userId` нет, отвечать обычным способом через ИИ

### Этап 2: Backend - Создание API endpoint

**Шаги:**
1. Создать папку `next/app/api/chat/`
2. Создать файл `route.ts`
3. Реализовать POST handler:
   - Получение `message` из body
   - Получение `userId` из cookies
   - Получение `conversationHistory` из body (опционально)
   - Валидация данных
   - Вызов `chatWithAI` через динамический импорт
   - Возврат ответа или ошибки

**Обработка ошибок:**
- Валидация входных данных
- Обработка ошибок от ИИ API
- Логирование ошибок

### Этап 3: Frontend - Обновление Store

**Шаги:**
1. Открыть `next/app/stores/useAssistantStore.ts`
2. Расширить интерфейс `AssistantState`
3. Добавить массив `messages`
4. Реализовать функции:
   - `addMessage` - генерация ID, добавление timestamp
   - `sendMessage` - async функция для отправки на API
   - `clearMessages` - очистка массива
   - `setLoading` и `setError`

**Особенности `sendMessage`:**
- Установка `isLoading = true`
- Добавление сообщения пользователя в историю
- Отправка POST запроса на `/api/chat`
- Добавление ответа ассистента в историю
- Обработка ошибок
- Установка `isLoading = false`

### Этап 4: Frontend - Обновление компонента

**Шаги:**

1. **Область сообщений:**
   - Заменить статичное приветствие на динамический список
   - Использовать `messages.map()` для отображения
   - Разные стили для `user` и `assistant`
   - `useEffect` для скролла к последнему сообщению
   - Показывать индикатор загрузки при `isLoading`

2. **Форма ввода:**
   - Добавить `useState` для значения input
   - Обработчик `onSubmit` (форма) или `onKeyDown` (Enter)
   - Вызов `sendMessage` из store
   - Блокировка input при `isLoading`
   - Очистка input после отправки

3. **Обработка ошибок:**
   - Отображение ошибок из store
   - Toast уведомления (опционально)

4. **Инициализация:**
   - При первом открытии добавить приветственное сообщение
   - Очистка истории при закрытии (опционально)

---

## 📝 Примеры кода

### Пример 1: Функция chatWithAI

```javascript
// Ключевые слова для определения запросов на рекомендации
const RECOMMENDATION_KEYWORDS = ['подскажи', 'посоветуй', 'порекомендуй'];

/**
 * Проверяет, содержит ли сообщение запрос на рекомендации
 */
function isRecommendationRequest(message) {
  const lowerMessage = message.toLowerCase();
  return RECOMMENDATION_KEYWORDS.some(keyword => lowerMessage.includes(keyword));
}

export async function chatWithAI(message, userId = null, conversationHistory = []) {
  // Если есть запрос на рекомендации и userId, используем функцию рекомендаций
  if (isRecommendationRequest(message) && userId) {
    try {
      const recommendations = await getTrackRecommedations(userId, 5);
      return `Вот 5 треков, которые могут тебе понравиться:\n\n${recommendations}`;
    } catch (error) {
      // Если ошибка при получении рекомендаций, продолжаем обычный чат
      console.error('Error getting recommendations:', error);
    }
  }

  // Обычная логика чата
  const systemPrompt = userId 
    ? 'Ты музыкальный ассистент. У тебя есть доступ к данным пользователя. Помогай с рекомендациями, анализом вкусов и статистикой.'
    : 'Ты музыкальный ассистент. Помогаешь пользователям с рекомендациями музыки, анализом вкусов и статистикой.';

  const messages = [
    { role: 'system', content: SYSTEM_PROMPT + ' ' + systemPrompt },
    ...conversationHistory,
    { role: 'user', content: message }
  ];

  const apiResponse = await client.chat.completions.create({
    model: 'xiaomi/mimo-v2-flash:free',
    messages,
  });

  return apiResponse.choices?.[0]?.message?.content || 'Не удалось получить ответ от ИИ.';
}
```

### Пример 2: API Route

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { cookies } from 'next/headers';

export async function POST(request: NextRequest) {
  try {
    const { message, conversationHistory = [] } = await request.json();
    
    if (!message || typeof message !== 'string') {
      return NextResponse.json(
        { error: 'Message is required' },
        { status: 400 }
      );
    }

    const cookieStore = await cookies();
    const userIdCookie = cookieStore.get('user_id');
    const userId = userIdCookie ? parseInt(userIdCookie.value) : null;

    const { chatWithAI } = await import('@/../../server/src/services/aiService.mjs');
    const response = await chatWithAI(message, userId, conversationHistory);

    return NextResponse.json({ response });
  } catch (error) {
    console.error('Chat API error:', error);
    return NextResponse.json(
      { error: 'Failed to process message' },
      { status: 500 }
    );
  }
}
```

### Пример 3: Обновленный Store

```typescript
import { create } from "zustand";

interface Message {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  timestamp: Date;
}

interface AssistantState {
  isOpen: boolean;
  messages: Message[];
  isLoading: boolean;
  error: string | null;
  toggleAssistant: () => void;
  closeAssistant: () => void;
  addMessage: (message: Omit<Message, 'id' | 'timestamp'>) => void;
  sendMessage: (content: string) => Promise<void>;
  clearMessages: () => void;
  setLoading: (loading: boolean) => void;
  setError: (error: string | null) => void;
}

export const useAssistantStore = create<AssistantState>((set, get) => ({
  isOpen: false,
  messages: [],
  isLoading: false,
  error: null,

  toggleAssistant: () => set((state) => ({ isOpen: !state.isOpen })),
  closeAssistant: () => set({ isOpen: false }),

  addMessage: (message) => {
    const newMessage: Message = {
      ...message,
      id: Date.now().toString(),
      timestamp: new Date(),
    };
    set((state) => ({
      messages: [...state.messages, newMessage],
    }));
  },

  sendMessage: async (content) => {
    const { addMessage, setLoading, setError } = get();
    
    // Добавляем сообщение пользователя
    addMessage({ role: 'user', content });
    setLoading(true);
    setError(null);

    try {
      const conversationHistory = get().messages
        .slice(-10) // Последние 10 сообщений для контекста
        .map(msg => ({ role: msg.role, content: msg.content }));

      const response = await fetch('/api/chat', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          message: content,
          conversationHistory,
        }),
      });

      if (!response.ok) {
        throw new Error('Failed to send message');
      }

      const data = await response.json();
      addMessage({ role: 'assistant', content: data.response });
    } catch (error) {
      setError(error instanceof Error ? error.message : 'Unknown error');
      addMessage({
        role: 'assistant',
        content: 'Извините, произошла ошибка. Попробуйте еще раз.',
      });
    } finally {
      setLoading(false);
    }
  },

  clearMessages: () => set({ messages: [] }),
  setLoading: (loading) => set({ isLoading: loading }),
  setError: (error) => set({ error }),
}));
```

### Пример 4: Примеры использования рекомендаций

**Сценарий 1: Пользователь с userId пишет "подскажи что послушать"**
```
Пользователь: "подскажи что послушать"
→ Система определяет ключевое слово "подскажи"
→ Вызывается getTrackRecommedations(userId, 5)
→ Возвращается: "Вот 5 треков, которые могут тебе понравиться:\n\nArtist1 - Track1\nArtist2 - Track2\n..."
```

**Сценарий 2: Пользователь без userId пишет "посоветуй музыку"**
```
Пользователь: "посоветуй музыку"
→ Система определяет ключевое слово "посоветуй"
→ Но userId = null, поэтому работает обычный чат
→ ИИ отвечает общими рекомендациями через chatWithAI
```

**Сценарий 3: Обычный вопрос без ключевых слов**
```
Пользователь: "какая у меня статистика?"
→ Нет ключевых слов для рекомендаций
→ Работает обычный чат через chatWithAI
```

---

## ✅ Чеклист реализации

### Backend
- [x] Добавить функцию `chatWithAI` в `aiService.mjs`
- [x] Реализовать функцию `isRecommendationRequest` для определения запросов на рекомендации
- [x] Интегрировать вызов `getTrackRecommedations` при обнаружении ключевых слов
- [x] Реализовать поддержку истории разговора
- [x] Добавить опциональную персонализацию через userId
- [x] Создать API endpoint `/api/chat`
- [x] Добавить валидацию входных данных
- [x] Реализовать обработку ошибок

### Frontend
- [x] Расширить `useAssistantStore` с новыми полями
- [x] Реализовать функцию `sendMessage`
- [x] Обновить компонент `Assistant.tsx`
- [x] Добавить отображение истории сообщений
- [x] Реализовать форму отправки сообщений
- [x] Добавить индикатор загрузки
- [x] Реализовать обработку ошибок
- [x] Добавить скролл к последнему сообщению
- [x] Добавить приветственное сообщение при открытии

### Тестирование
- [ ] Проверить отправку сообщений
- [ ] Проверить получение ответов от ИИ
- [ ] **Проверить автоматический вызов рекомендаций при словах "подскажи/посоветуй/порекомендуй"**
- [ ] Проверить работу рекомендаций без userId (должен работать обычный чат)
- [ ] Проверить сохранение истории
- [ ] Проверить обработку ошибок
- [ ] Проверить работу с userId
- [ ] Проверить UI/UX

---

## 🚀 Дополнительные улучшения (опционально)

1. **Персонализация:**
   - Использовать данные пользователя из БД для более точных ответов
   - Контекст о любимых треках, жанрах и т.д.

2. **Улучшения UX:**
   - Анимации появления сообщений
   - Индикатор печати (typing indicator)
   - Звуковые уведомления
   - Автосохранение истории в localStorage

3. **Функциональность:**
   - Команды для быстрых действий (например, "покажи рекомендации")
   - Поддержка мультимедиа (ссылки на треки)
   - Экспорт истории разговора

4. **Производительность:**
   - Дебаунсинг для отправки сообщений
   - Кэширование частых запросов
   - Оптимизация размера истории

---

## 📚 Полезные ссылки

- [Next.js API Routes](https://nextjs.org/docs/app/building-your-application/routing/route-handlers)
- [Zustand Store](https://zustand-demo.pmnd.rs/)
- [OpenAI API](https://platform.openai.com/docs/api-reference)
- [Framer Motion](https://www.framer.com/motion/)

---

## ⚠️ Важные замечания

1. **Безопасность:**
   - Валидировать все входные данные
   - Ограничить длину сообщений
   - Защита от спама (rate limiting)

2. **Производительность:**
   - Ограничить размер истории разговора (например, последние 20 сообщений)
   - Оптимизировать промпты для экономии токенов

3. **Ошибки:**
   - Всегда обрабатывать ошибки gracefully
   - Показывать понятные сообщения пользователю
   - Логировать ошибки для отладки

---

**Готово к реализации!** 🎉
